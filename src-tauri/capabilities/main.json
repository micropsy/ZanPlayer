{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main capability for Sub Player app",
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "http:default",
    "shell:allow-open",
    "updater:default"
  ]
}
